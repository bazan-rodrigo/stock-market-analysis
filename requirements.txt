dash>=2.14.0
dash-bootstrap-components>=1.5.0
Flask>=3.0.0
Flask-Login>=0.6.3
SQLAlchemy>=2.0.0
alembic>=1.13.0
yfinance>=0.2.36
pandas>=2.1.0
numpy>=1.26.0
bcrypt>=4.1.0
APScheduler>=3.10.4
openpyxl>=3.1.2
plotly>=5.18.0
gunicorn>=21.2.0

# El DRIVER de base de datos NO va acá: depende del motor elegido al instalar
# y vive en requirements-postgres.txt / requirements-mysql.txt. Instalar los
# dos significa compilar una extensión en C (mysqlclient) para no usarla.
#     pip install -r requirements.txt -r requirements-<motor>.txt
# En Railway lo agrega railpack.json extendiendo el paso de install.
