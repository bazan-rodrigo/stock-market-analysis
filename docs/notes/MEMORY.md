# Memoria del proyecto

- [IA con cuenta propia del usuario — MCP](project_ia_mcp.md) — 1-ago (solo diseño, sin código): **DECIDIDO MCP con el cliente del usuario**; panel in-app DESCARTADO por ahora (era el único que obligaba a guardar credenciales), servicio APARTE en Railway, todos los roles, escritura solo por packs. Hallazgos: el **gate de visibilidad vive en la UI y no se hereda** (`current_viewer` depende de flask_login, los servicios no filtran) — el mayor riesgo; backtest niveles B y C **ya son efímeros**, solo el A persiste; y **lo que ensucia no son los runs sino materializar la estrategia** (`strat_res_{id}` + backfill real en producción por cada variante). **PENDIENTE: el usuario investiga el backtest antes de seguir**

- [Indicadores nuevos: posición 52W, ADX y volumen relativo (0098)](project_indicadores_0098.md) — 31-jul/1-ago (05edf82, 1227 passed): de 10 propuestas de otra IA salieron 3; `distance_all_time_high` YA ERA `drawdown_current` y `breakout_score` es una estrategia disfrazada de indicador. **El hallazgo no fue un indicador**: el score de estrategia **RENORMALIZA** ante dato faltante mientras el filtro **EXCLUYE** — el activo sin dato compite con otra fórmula y le va sistemáticamente mejor; recién importa ahora que `rvol_daily` es el primer indicador de cobertura parcial (sintéticos sin volumen, decisión del usuario). Criterio de admisión del catálogo: escalar **comparable entre activos**, no "un solo valor" — por eso un compuesto entra si se persiste su REDUCCIÓN. Medido: +26-30% del cómputo de backfill, 84% es el ADX. **PENDIENTE Railway: upgrade 0098 + recálculo completo**

- [Indicadores con historia: Drawdown % y ATR % (0097)](project_indicadores_con_historia.md) — 27-jul (14b124c, 1181 passed): faltaban en Posicionamiento Histórico por **tres motivos distintos** (solo-JS del gráfico / `keep_history=False` / categóricos que el filtro `type=="num"` descarta). Nuevos `drawdown_pct_daily` + `atr_pct_*` (ATR **normalizado**, el absoluto no sirve en un histograma), ambos en `_CHECKSUM_DEP_CODES`. Ojo: `_atr_pct_series_v` era el PERCENTIL (renombrado). `check_sanity` no chequea EN SILENCIO un código sin límites → trinquete nuevo. **VERIFICADO en Railway**. **Hilo CERRADO**: las 3 continuaciones (categóricos, S/R con historia, MACD/Estocástico/Bollinger) están DESCARTADAS — no re-proponerlas

- [Packs como estándar publicado (para personas y otras IA)](project_packs_estandar.md) — 26-jul: SPEC.md v1 (fijo) + catálogo exportable por instalación (variable) + validador offline; el import acepta JSON además de xlsx y resuelve **atributos por nombre** (era LA barrera de portabilidad); `test_pack_spec.py` ata el documento al código. **31-jul (19e3bc5, 1209 passed): el SPEC se desfasó en 4 días y el trinquete no lo vio — ataba las LISTAS, no la PROSA** (§1 flujo viejo, §8 "todo o nada" falso, §6 sin fecha-exacta/45d); README decía `publica` ausente = pública (es PRIVADA). Los 4 packs ya están en JSON canónico vía `scripts/pack_to_json.py` (round-trip verificado celda por celda) + trinquete de que ambos formatos coincidan. Pendiente verificar en Railway

- [Sistema de diseño único de UI + trinquete](project_sistema_diseno_ui.md) — 26-jul (e37da72): el dolor no era Dash sino la heterogeneidad entre pantallas; ui_constants pasa a sistema global (colores sueltos 544→178), page_header sin `level`, test_ui_consistency.py como red. **custom.css y dark_theme.js = ZONA INTOCABLE** (ahí viven los fixes de legibilidad ganados por especificidad). Incluye el acoplamiento a Dash medido por si vuelve el tema de migrar

- [Grillas camino a 10.000 activos — DevExtreme descartado, TODA la app en ag-grid](project_grillas_10k.md) — 26/27-jul: los gráficos no eran el problema; eran las grillas. Etapa 0 (topes + LIMIT real en SQL) y **migración COMPLETA**: 20 grillas en 19 pantallas, cero `dash_table.DataTable`, `table_styles.py` borrado (c1cdd12 + 0af810d, 1155 passed). Ojo ag-grid 35: el tema default es CLARO y se configura desde JS → hace falta `theme: "legacy"`. **Render VERIFICADO en Railway** (27-jul: se ve ok); falta ejercitar los caminos de selección (borrar/editar). Medir antes de la etapa 3

- [Señales/estrategias a tablas anchas — CUTOVER HECHO Y VALIDADO](project_sig_wide_tables.md) — 25/26-jul: **las `sig_{id}`/`strat_res_{id}` YA NO EXISTEN** — dropeadas por la 0094, flag `USE_WIDE_SIGNAL_TABLES` **default ON** (7d2eb2c), validado end-to-end en Railway con datos reales. Hoy: `signal_values_wide` (col `sig_{id}`) + `strategy_results_wide` (`strat_{id}_score`/`_pct`); se leen por `read_sig_table()`/`read_strat_table()` (subquery con IS NOT NULL horneado, drop-in). Medido 3,7× en señales (373,7 MB vs ~1,34 GB), estrategias 0,9× a N=1 (el ahorro llega con 2+). **Consecuencia viva: crear/borrar una señal o estrategia es ALTER TABLE ADD/DROP COLUMN sobre una tabla COMPARTIDA** (lock sobre lo que usa todo el pipeline; en PG la col dropeada queda muerta hasta el rewrite), ya no CREATE/DROP TABLE. Bug arreglado (c9765ab): el reconcile per-entidad revivía las viejas en cada arranque

- [Resiliencia/monitoreo ante corte de corrida](project_resiliencia_corridas.md) — 25/26-jul: (1) **bug del rebuild interrumpido ARREGLADO** (df1a0ea: `_initial_cleanup` preservaba `signal_eval_log` → hueco permanente silencioso; fix = borrar markers del alcance + re-marcar siempre en force; test de regresión). (2) **Monitoreo M1+M2 HECHOS** (7663e75, migración 0096, 936 passed): tabla `run_history` persistida + `run_history_service` (fail-open); `start_run`/`finish_run` en `_run`/scheduler, `abort_orphans`+`prune_old(180d)` al arranque, UI "Historial de corridas". **PENDIENTE Railway: `alembic upgrade head`** (ambos). DIFERIDO: atomicidad total (build-and-swap) y M3 (chequeo de cobertura precios vs fechas evaluadas)

- [Reinicio TOTAL a estado de fábrica en Limpieza de datos](project_reset_fabrica.md) — 25-jul: botón + `scripts/clean_data.py --reset` que dejan la base como recién instalada (borra TODO incl. activos/definiciones/usuarios, resiembra lo integrado, recrea admin/admin123). `reset_to_fresh_install` + `db_compat.truncate_all_tables` (PG TRUNCATE CASCADE resuelve el ciclo assets↔markets). Doble confirmación (checkbox + tipear REINICIAR), lock HEAVY_WRITE. 906 passed. **VERIFICADO en Railway** (25-jul, a86b4d5+ea96e28: el usuario lo corrió en producción y funcionó OK)

- [Removidas señales de grupo + Alcance en estrategias](project_remover_grupo.md) — 24-jul: el usuario las REMUEVE para que los usuarios reales no las usen (barato solo porque nadie las usa; usarlas enciende el bloat de group_scores). group_scores + Mapa de Mercado SOBREVIVEN (se calculan desde ind_trend_*, no desde señales). Paso 1 (UI gate, 19ac3c7 pusheado) + Paso 2 (raíz: −1000 líneas, migración 0090 dropea group_signal_value + columnas source/group_type/scope). Opción (a): el backfill conserva group_scores última fecha; no toqué el mapa (otra sesión). Import RECHAZA source=group/scope. 904 passed. **PENDIENTE: `alembic upgrade head` en Railway**

- [Mapa de Tendencia: scores de grupo AL VUELO](project_score_semanal_mensual_mapa.md) — 25-jul (f4aa785, 913 passed): el mapa calcula al vuelo desde ind_trend_* con selector de fecha, ya NO persiste group_scores (único consumidor). Diario as-of (últ. `<=target`, 45d) + sem/men covering (1ª `>=target`, 40d) → arregla de una el sem/men vacío Y el diario vacío en Sectores los fin de semana. Se removieron compute_group_scores/run_daily/_refresh_group_scores y la escritura en signal_backfill_range. **Fase 3 HECHA (b8e6d43): migración 0092 dropea la tabla, fuera el modelo GroupScore + refs (data_explorer/cleanup/maintenance); 912 passed.** PENDIENTE en Railway: `alembic upgrade head` (verificar head, la sesión paralela suma migraciones sig-wide) + verificar mapa+selector

- [Reducción de footprint en disco](project_reduccion_footprint.md) — 23-jul: **RONDA 1 CERRADA y MEDIDA en Railway: 2,5 → 2,3 GB.** #2 float4 indicadores (−148, 0087); #1 prices.id→PK compuesta (−169 en prices: 97 índice muerto + 72 recompactar, 0089 dual-safe); #4 float4 en sig_* NO rinde (padding MAXALIGN de PG). Las señales (1,2 GB, 50%) intactas → solo #5/#6 (pausados). Falta verificar 1 día de scores (float4)

- [Corridas del Centro de Datos en el proceso web](project_corridas_proceso_web.md) — 22-jul (00e61da): gunicorn `--timeout 120` mataba las corridas largas sin dejar error; subido a 1800 y **VERIFICADO en Railway** (redescarga global de 3,36M filas completó, y autovacuum corriendo sobre `prices` durante la corrida = el xmin horizon ya no queda fijado). Railway real: 8 vCPU / 8 GB, `max_connections=500`. ProcessPool CONFIRMADO activo (`(True, 4)`); migrar a worker.py queda DIFERIDO — el `--timeout 1800` es un parche que vuelve a apretar hacia los 10.000 activos, y el arreglo real es un proyecto (progreso en memoria, miles de llamados por corrida)

- [Soporte dual: SE MANTIENE — el corte a PG-only se DESCARTÓ](project_postgres_only_estudio.md) — 23-jul: **NO borrar ramas de MySQL**; el motor es una elección de INSTALACIÓN (ambas opciones se conservan), hoy Railway+PG; el corte murió cuando se verificó que su dependencia B→C era **una sola línea** y no habilitaba nada; SOBREVIVEN la etapa A (hecha: lock_timeout, login determinista, unicidad de usuario) y la cosecha PG (COPY/CLUSTER/LATERAL), que **no exige cortar nada**
- [Carga masiva de activos a alto volumen — IMPLEMENTADO](project_carga_masiva_alto_volumen.md) — 21-jul (adcd0bb/ea97fa5): import Excel en 2 fases (validación paralela + salteo del .info con fila completa) y "solo nuevos"/redescarga global por el camino batch compartido; pendiente probar con red real en Codespace
- [AVISO: módulo de limpieza commiteado por error desde otra sesión](project_cleanup_commiteado_por_error.md) — sus archivos entraron en el commit f626f01 (mensaje engañoso: dice "docs(notes)"); ya desplegado en Railway; NO reescribir historia (decidido); **no usar `git add -A` en este repo, hay sesiones en paralelo**; nota transitoria, borrable al cerrar el módulo

- [Manual de usuario web](project_manual_usuario.md) — 59 secciones en docs/manual/*.md renderizadas en /manual, filtrado por rol; tests atan el manual al código (pantalla nueva sin documentar = suite rota); al escribirlo salieron 17 bugs del código, 13 arreglados (f4fab61); **el modo invitado se ELIMINÓ (5abd63a): login siempre obligatorio, migración 0086 pendiente de aplicar en Codespace/Railway**
- [Promover cartera hereda la config gated — IMPLEMENTADO](project_hallazgo_promover_cartera.md) — 26-jul: el promover congela la corrida gated que se ve (reglas+rebalanceo+costo) como PortfolioRun y la vincula (sim_spec+source_run_id, migración 0095); /carteras dibuja esa curva + botón Recalcular (thread+lock). Fuera de v1: auto-avance por scheduler. **PENDIENTE: `alembic upgrade head` en Railway + verificar el flujo vivo**

- [Módulo de backtesting](project_backtest.md) — REDISEÑO COMPLETO (19-jul): Backtest niveles A-D (Señal/Reglas/Cartera/Comparar+Walk-forward) + Carteras (reales con registro de operaciones, teóricas curadas/derivadas de estrategia, vínculo real→teórica); migraciones 0070/0080/0083/0084; walk-forward objetivo Sharpe + carga batcheada; método "regla dinámica" descartado (redundante con derivada de estrategia)
- [Tabla por señal/estrategia — IMPLEMENTADO](project_tablas_por_senal.md) — sig_{id}/strat_res_{id} como ind_{code} (16-jul, 992fe3e, migración 0075 copia historia); verificar en Codespace y medir; siguiente: paralelización

- [Proyecto: Stock Market Analysis](project_overview.md) — App web Dash+Flask para análisis técnico de activos financieros, con admin y analistas
- [Decisiones técnicas acordadas](project_decisions.md) — APScheduler proceso único, Alembic, screener pre-calculado (nomenclatura current/history/scores), Yahoo Finance only, admin hardcoded
- [Suite de tests pytest](project_testing.md) — 914 tests de lógica pura en tests/ (incl. portfolio_metrics/views/service/sim_engine + backtest cartera/reglas/walk-forward + import/price/fundamental bulk); correr `pytest` antes de cada push (el venv local ya tiene las deps)
- [Pendientes próxima sesión](project_pendientes.md) — log sesión por sesión (más reciente arriba); al 22-jul: "Recalcular caché" YA CORRIDO en Railway (remediaba el bug del flush ancho: `ind_asset_meta` de filas inexistentes; falta la verificación de datos), **etapa 0 del corte PG-only** (aplicar 0086 + usuarios duplicados sin distinguir caso — si hay un par, editarlos falla), verificaciones de la etapa A en Codespace, y los tokens de GitHub en texto plano en los remotes
- [Objetivo: soportar 10000 activos](project_scaling_target.md) — **MÉTODO en 4 pasos** (cProfile=dónde / leer código=qué / medir en la base=cuánto / verificar que no cambie el resultado); cProfile infla 3.7-4.2x acá, medir con `--raw` y nunca en Railway; 4 optimizaciones validadas y 2 revertidas por medición; el bloat del backfill por-activo (técnico Y fundamental) era el hallazgo grande. build_panels cerrado (fc0fb03: 1.35x a 500 activos, bit-identico). Patrón reusable: `scripts/bench_*.py` (ambas versiones, mismo proceso, datos reales, verificando igualdad de salidas). 22-jul: **MEDIDO el camino real y el A/B del pool** (`profile_indicator_delta_real.py` + `bench_ind_pool_procs.py`, Railway, deriva 1.2%): delta completo **105.63 ms/activo con 4 procs → ~775-1056s a 10k, ENTRA en el timeout de 1800s**. **El pool SÍ escala: 3.16x con 4 procs** (threads 333.77 → 105.63 ms/activo); quedarse en 4, subir a 6 solo compra 12%. **TRES estimaciones mías previas fueron malas** (~140s, ~2265s, "el pool compra 1.0-1.15x") — dos por comparar containers distintos de Railway, una por armar el baseline sumando mediciones aisladas: **las partes aisladas no suman el todo (subestimaban 2.8x)**. El cuello a 10k es la parte serial (~15s, con los full-scans de tail_stats que crecen), no los procesos
- [Tablas anchas de indicadores por cadencia](project_ind_wide_tables.md) — REFACTOR COMPLETO (código): fases 1-5, wide es el default, ~5.5x menos footprint (ind_* ~22.8→~4.1 MB), migraciones 0077/0078/0079; falta aplicar la 0079 (drop) en Railway; diseño en docs/notes/design_ind_wide_tables.md
- [Filtro de estrategias + roadmap indicadores](project_filtro_estrategias.md) — Filtro AND/OR + editor de señales + backfill delta, todo en vivo (12-jul); semántica as-of de indicadores; próximo natural: backtest por deciles; indicadores por plantilla diferido
- [group_scores solo para grupos consumidos](project_group_scores_scope.md) — 13-jul (f5b396f): group_scores/group_signal_value se calculan solo para los grupos que una estrategia usa (derivado del filtro). **SUPERADO (24-jul): las señales de grupo se REMOVIERON — ver [[remover-senales-grupo-y-alcance]]; ya no hay derivación ni group_signal_value**
- [Scores en días sin precio propio — SIN DECIDIR](project_scores_dias_sin_precio.md) — as-of arrastra scores a fechas no cotizadas; 2 alternativas guardadas (gate vs flag preliminar) en docs/notes/design_scores_dias_sin_precio.md, retomar
- [Entorno de verificación: SOLO Railway](feedback_entorno_verificacion.md) — 22-jul: el **Codespace ya no se usa**; verificar = producción, no hay entorno descartable; los `profile_*` que escriben deben tomar el `run_lock`
- [Motor de base de datos: elección de INSTALACIÓN](feedback_mariadb.md) — 23-jul: PostgreSQL o MySQL, elegido al instalar y válido en cualquier entorno (no es propiedad del entorno); hoy Railway+PG pero **el dual se mantiene**; no instalar los dos motores si se usa uno
- [Modal no se cierra si hay error al guardar](feedback_modal_on_error.md) — El modal ABM debe permanecer abierto en error; solo el callback de save cierra el modal (en éxito)
- [Registro de pantallas nuevas](feedback_registro_pantallas.md) — Sin auto-discovery: _PAGES + _CALLBACKS en app/__init__.py + navbar; test_module_registration.py es la red
- [Reflejar todo cambio en la UI y en el SPEC](feedback_reflejar_en_ui_y_spec.md) — 26-jul: motor + interfaz + contrato publicado (SPEC/manual) son tres caras de lo mismo, se tocan en el mismo commit; lo que la UI no ofrece es invisible y lo que el SPEC no documenta nadie lo puede usar desde afuera
- [Un indicador nuevo se ve en el GRÁFICO](feedback_indicador_se_ve_en_el_grafico.md) — 1-ago: agregar un indicador incluye su panel en Gráfico Técnico, no es trabajo aparte. **`_SLOTS` NO es la lista de lo que el gráfico dibuja** (es solo lo que se calcula en JS): ya hay 5 overlays servidos desde Python, incl. régimen de tendencia y `rs52w`, que lee `ind_daily`. Molde a copiar: `load_rs52w_overlay`. Ya se perdió dos veces

- [Idioma de comunicación](feedback_language.md) — Responder siempre en español
- [Pedir confirmación antes de aplicar cambios](feedback_confirmacion_cambios.md) — Presentar la solución y esperar "sí" antes de editar archivos
- [Estrategias como archivos de import](feedback_strategy_packs.md) — Cuando pide una estrategia, generar los xlsx en strategy_packs/, no pasos manuales
- [Popup calendario DatePicker — no tocar](feedback_calendar_popup.md) — Fondo blanco del popup no resuelto tras múltiples intentos; usuario decidió dejarlo
- [Migración a PostgreSQL — FASES 1-4 HECHAS + equivalencia validada](project_postgresql_migracion.md) — 16-jul: db_compat, bootstrap create_all/stamp, semántica, entorno DB_ENGINE; `alembic check` limpio en Codespace tras alinear índices (3970763); comparador de paridad listo (c2652db); falta ejecutar fase 5 (paridad both, Codespace descartable) y fase 6 (datos reales)
- [ProcessPool con partición por activos](project_processpool_particion_activos.md) — Diseño elegido para escalar el pool de indicadores (resuelve GIL + caché a 10k activos); se encara junto con PostgreSQL
