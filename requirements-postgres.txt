# Driver de PostgreSQL. Se instala JUNTO a requirements.txt:
#     pip install -r requirements.txt -r requirements-postgres.txt
# En Railway lo agrega railpack.json (paso "install" extendido).
psycopg[binary]>=3.1
